/**
 * Chapter indexing is opt-in by default.
 *
 * A project only starts background indexing after the user explicitly chooses
 * to continue or rebuild the index, which persists an enabled project switch.
 */
export const DEFAULT_BACKGROUND_INDEX_ENABLED = false;
